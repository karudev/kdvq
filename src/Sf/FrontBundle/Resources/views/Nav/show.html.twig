<!-- -->
<nav class="container hidden-md hidden-sm hidden-lg">
    <div class="row navbar">
        <div class="col-md-12 content">
            <div class="sidebar-nav">
                <div class="navbar navbar-default" role="navigation">
                    <div class="navbar-header">
                        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".sidebar-navbar-collapse">
                            <span class="sr-only">Toggle navigation</span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                        </button>
                        <span class="visible-xs navbar-brand"><img src="images/logo_sm.png" alt=""> qwinsport</span>
                    </div>
                    <div class="navbar-collapse collapse sidebar-navbar-collapse">
                        <ul class="nav navbar-nav">
                            <li class="active"><a href="{{path('front_home')}}">Accueil</a></li>
                                {% for c in categories %}

                                {% if c.categories|length > 0 %}
                                    <li class="dropdown">
                                        <a href="{{path('front_category',{'slug':c.slug})}}" class="dropdown-toggle" data-toggle="dropdown">{{c.name}} <b class="caret"></b></a>
                                        <ul class="dropdown-menu">
                                            {% for sc in c.categories %}
                                                {% if sc.custom == false %}
                                                    <li><a href="{{path('front_products',{'categorySlug':sc.slug})}}">{{sc.name}}</a></li>
                                                    {% endif %}
                                                {% endfor %}
                                        </ul>
                                    </li>
                                {% else %}
                                    <li><a href="{{path('front_category',{'slug':c.slug})}}">{{c.name}}</a></li>
                                    {% endif %}
                                {% endfor %}


                            <li><a href="#">Personnaliser</a></li>
                            <li><a href="{{path('front_actus')}}">Actus</a></li>
                            <li><a href="{{path('front_partners')}}">Clubs</a></li>
                            <li><a href="{{path('front_press')}}">Presse</a></li>
                            <li><a href="{{path('front_partners')}}">Partenaires</a></li>
                                {% if is_granted('ROLE_ADMIN') %}
                                <li class="dropdown">
                                    <a href="#" class="admin dropdown-toggle" data-toggle="dropdown">Admin <b class="caret"></b></a>
                                    <ul class="dropdown-menu">
                                        <li><a href="{{path('admin_accounts')}}">Les comptes</a></li>
                                        <li><a href="{{path('orders')}}">Les commandes</a></li>
                                        <li><a href="{{path('admin_product')}}">Les Produits</a></li>
                                        <li><a href="{{path('admin_press')}}">La Presse</a></li>
                                        <li><a href="#">Configuration</a></li>
                                    </ul>
                                </li>
                            {% endif %}
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</nav>

<div class="col-md-2 col-sm-3">
    <div class="sidebar-nav hidden-xs pc">
        <div class="navbar navbar-default" role="navigation">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".sidebar-navbar-collapse">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <span class="visible-xs navbar-brand"><img src="images/logo_sm.png" alt=""> qwinsport</span>
            </div>
            <div class="navbar-collapse collapse sidebar-navbar-collapse">
                <ul class="nav navbar-nav">
                    <li class="active"><a href="{{path('front_home')}}">Accueil</a></li>
                        {% for c in categories %}
                            {% if c.categories|length > 0 %}
                            <li class="dropdown">
                                <a href="{{path('front_category',{'slug':c.slug})}}" class="dropdown-toggle" data-toggle="dropdown">{{c.name}} <b class="caret"></b></a>
                                <ul class="dropdown-menu">
                                    {% for sc in c.categories %}
                                        {% if sc.custom == false %}
                                            <li><a href="{{path('front_products',{'categorySlug':sc.slug})}}">{{sc.name}}</a></li>
                                            {% endif %}
                                        {% endfor %}
                                </ul>
                            </li>
                        {% else %}
                            <li><a href="{{path('front_category',{'slug':c.slug})}}">{{c.name}}</a></li>
                            {% endif %}
                        {% endfor %}
                    <li><a href="#">Personnaliser</a></li>
                    <li><a href="{{path('front_actus')}}">Actus</a></li>
                    <li><a href="{{path('front_partners')}}">Clubs</a></li>
                    <li><a href="{{path('front_press')}}">Presse</a></li>
                    <li><a href="{{path('front_partners')}}">Partenaires</a></li>
                        {% if is_granted('ROLE_ADMIN') %}
                        <li class="dropdown">
                            <a href="#" class="admin dropdown-toggle" data-toggle="dropdown">Admin <b class="caret"></b></a>
                            <ul class="dropdown-menu">
                                <li><a href="{{path('admin_accounts')}}">Les comptes</a></li>
                                <li><a href="{{path('orders')}}">Les commandes</a></li>
                                <li><a href="{{path('admin_product')}}">Les Produits</a></li>
                                 <li><a href="{{path('admin_press')}}">La Presse</a></li>
                                <li><a href="#">Configuration</a></li>
                            </ul>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </div>
</div>